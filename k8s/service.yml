apiVersion: v1
kind: Service
metadata:
  name: firemuseum
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "firemuseum"
    port: 8080
  selector:
    app: firemuseum
